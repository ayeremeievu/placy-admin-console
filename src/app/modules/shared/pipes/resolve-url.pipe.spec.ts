import { ResolveUrlPipe } from './resolve-url.pipe';

describe('ResolveUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
